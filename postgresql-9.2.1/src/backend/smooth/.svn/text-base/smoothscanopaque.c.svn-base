/*
 * smoothscanopaque.c
 *
 *  Created on: May 22, 2013
 *      Author: renata
 */

#include "smooth/smoothscanopaque.h"


//
///* Save an index item into so->currPos.items[itemIndex] */
// void
//_bt_saveheapitem(SmoothScanOpaque ss, int itemIndex,
//			 OffsetNumber offnum, HeapTuple htup)
//{
//	SmoothScanPosItem *currItem = &ss->currPos.items[itemIndex];
//
//	currItem->heapTid = htup->t_self;
//	currItem->tuplePageOffset = offnum;
//
//	if (ss->currTuples)
//	{
//		Size tupleSize = HEAPTUPLESIZE + htup->t_len;
//		//Size tupleSize  HeapTupleHeaderGetDatumLength(htup->t_data);
//		currItem->tupleOffset = ss->currPos.nextTupleOffset;
//		memcpy(ss->currTuples + ss->currPos.nextTupleOffset, htup, tupleSize);
//		ss->currPos.nextTupleOffset += MAXALIGN(tupleSize);
//	}
//
//
//}

